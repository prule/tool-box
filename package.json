{
  "name": "toolbox",
  "version": "0.1.0",
  "description": "A collection of essential developer tools available as a simple, static website.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=19"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
